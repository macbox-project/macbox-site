# macbox-site

The MacBox website: <https://mac.box/>.

Published to GitHub Pages by `.github/workflows/pages.yml` on every push to
`main`. `root/` is copied verbatim to the top of the published site: a bare
`index.html` and the `CNAME` for the custom domain.

**This repo is public. Anything committed is published.**
